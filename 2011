class Solution {
    public int finalValueAfterOperations(String[] operations) {
        int ans = 0;
        for (final String oper : operations){
            ans += oper.charAt(1) == '+' ? 1 : -1;   
        }
        return ans;

    }
}
